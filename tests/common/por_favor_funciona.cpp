#include "../include/third_party/doctest/doctest.h"
#include "../include/common/services/player_service.h"

TEST_CASE("Testando o doctest2") {
    CHECK(0==0);
}

TEST_CASE("Testando o doctest3") {
    CHECK(0==1);
}